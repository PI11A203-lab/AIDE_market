# 사이트 전체 관리자 권한 시스템 구현 가이드

## 📋 개요

현재 시스템에는 두 가지 권한만 존재합니다:
- `user`: 일반 사용자 (상품 구매)
- `admin`: 관리자 (상품 업로드 및 관리)

새로운 **사이트 전체 관리자(Site Admin)** 권한을 추가하여 사이트 전반을 관리할 수 있는 시스템을 구축합니다.

---

## 🎯 목표

1. **새로운 권한 레벨 추가**: `super_admin` (사이트 전체 관리자)
2. **사이트 전체 관리자 대시보드 생성**: 4개 주요 관리 페이지 통합
   - 📦 상품 관리 (admin이 신청한 상품 승인)
   - 🎓 학생 인증 관리 (학생 인증 승인/거부)
   - 📊 IP 관리 (접속자 수, 통계, 데이터화)
   - 🔒 보안 관리 (봇 차단, 보안 이벤트, 그래프화)
3. **권한 기반 접근 제어**: 각 권한별 접근 가능한 기능 분리
4. **데이터 시각화**: 통계 및 그래프를 통한 데이터 관리

---

## 📊 데이터베이스 변경사항

### 1. Users 테이블 수정

**현재 상태:**
```sql
role ENUM('admin', 'user') DEFAULT 'user'
```

**변경 후:**
```sql
role ENUM('super_admin', 'admin', 'user') DEFAULT 'user'
```

**변경 사항:**
- `super_admin`: 사이트 전체 관리자 (최고 권한)
- `admin`: 상품 관리자 (상품 업로드 및 관리)
- `user`: 일반 사용자 (구매)

### 2. 기존 데이터 마이그레이션

기존 `admin` 권한 사용자들은 그대로 유지하고, 필요시 `super_admin`으로 변경

**마이그레이션 SQL:**
```sql
-- 예시: id=1인 사용자를 super_admin으로 변경 (필요시)
UPDATE users SET role = 'super_admin' WHERE id = 1;
```

---

## 🔧 백엔드 변경사항

### 1. User 모델 수정

**파일:** `ECSite-server/GIT/ECSite-server/features/user/userModel.js`

```javascript
role: {
    type: DataTypes.ENUM('super_admin', 'admin', 'user'),
    allowNull: true,
    defaultValue: 'user',
}
```

### 2. 학생 인증 승인/거부 API 추가

**파일:** `ECSite-server/GIT/ECSite-server/features/user/studentAccountService.js`

```javascript
/**
 * 대기 중인 학생 인증 신청 목록 조회 (super_admin용)
 */
exports.getPendingStudentVerifications = async (transaction = null) => {
    const pendingUsers = await models.User.findAll({
        where: {
            account_type: 'student',
            student_verified_at: null,
            student_verification_document: {
                [models.sequelize.Op.ne]: null
            }
        },
        attributes: [
            'id',
            'username',
            'email',
            'account_type',
            'student_verified_at',
            'student_expires_at',
            'student_verification_document',
            'createdAt'
        ],
        order: [['createdAt', 'DESC']],
        transaction
    });

    return pendingUsers;
};

/**
 * 학생 인증 거부 (super_admin용)
 */
exports.rejectStudentVerification = async (userId, reason, transaction = null) => {
    const user = await models.User.findByPk(userId, { transaction });
    if (!user) {
        throw new Error('사용자를 찾을 수 없습니다');
    }

    if (!user.student_verification_document) {
        throw new Error('학생 인증 신청이 없습니다.');
    }

    // 인증 문서 삭제 (선택사항)
    // 또는 rejected 상태로 표시하기 위한 별도 필드 추가 고려

    await user.update({
        account_type: 'general',
        student_verification_document: null
    }, { transaction });

    return user;
};
```

### 3. 학생 인증 관리 API 엔드포인트

**파일:** `ECSite-server/GIT/ECSite-server/features/user/studentAccountController.js`

```javascript
/**
 * 대기 중인 학생 인증 신청 목록 조회 (super_admin용)
 */
exports.getPendingStudentVerifications = async (req, res) => {
    try {
        const pendingUsers = await studentAccountService.getPendingStudentVerifications();
        
        res.json({
            success: true,
            pendingVerifications: pendingUsers.map(user => ({
                id: user.id,
                username: user.username,
                email: user.email,
                documentPath: user.student_verification_document,
                appliedAt: user.createdAt
            }))
        });
    } catch (error) {
        console.error('대기 중인 학생 인증 목록 조회 실패:', error);
        res.status(500).json({
            success: false,
            error: error.message
        });
    }
};

/**
 * 학생 인증 거부 (super_admin용)
 */
exports.rejectStudentVerification = async (req, res) => {
    try {
        const { userId } = req.params;
        const { reason } = req.body;
        
        const transaction = await models.sequelize.transaction();
        
        try {
            const user = await studentAccountService.rejectStudentVerification(
                userId, 
                reason, 
                transaction
            );
            await transaction.commit();

            // 거부 이메일 발송 (선택사항)
            // await emailService.sendStudentRejectedEmail(user, reason);

            res.json({
                success: true,
                message: '학생 인증이 거부되었습니다.',
                user: user.toSafeJSON ? user.toSafeJSON() : user
            });
        } catch (error) {
            await transaction.rollback();
            throw error;
        }
    } catch (error) {
        console.error('학생 인증 거부 실패:', error);
        res.status(400).json({
            success: false,
            error: error.message
        });
    }
};
```

### 4. 라우트 추가

**파일:** `ECSite-server/GIT/ECSite-server/features/user/studentAccountRoutes.js`

```javascript
// 학생 인증 관리 (super_admin용)
router.get("/student-verifications/pending", studentAccountController.getPendingStudentVerifications);
router.post("/:userId/student-verification/reject", studentAccountController.rejectStudentVerification);
```

### 5. 권한 체크 미들웨어

**파일:** `ECSite-server/GIT/ECSite-server/middleware/auth.js` (신규 생성 또는 기존 파일 수정)

```javascript
/**
 * super_admin 권한 체크 미들웨어
 */
exports.requireSuperAdmin = async (req, res, next) => {
    try {
        // TODO: JWT 토큰에서 사용자 정보 추출
        // const user = await getUserFromToken(req);
        
        // 임시: 요청 헤더에서 사용자 ID 가져오기 (실제 구현시 JWT 사용)
        const userId = req.headers['user-id'];
        if (!userId) {
            return res.status(401).json({ error: '인증이 필요합니다.' });
        }

        const user = await models.User.findByPk(userId);
        if (!user || user.role !== 'super_admin') {
            return res.status(403).json({ error: '사이트 관리자 권한이 필요합니다.' });
        }

        req.user = user;
        next();
    } catch (error) {
        console.error('권한 체크 실패:', error);
        res.status(500).json({ error: '권한 확인 중 오류가 발생했습니다.' });
    }
};

/**
 * admin 또는 super_admin 권한 체크 미들웨어
 */
exports.requireAdmin = async (req, res, next) => {
    try {
        const userId = req.headers['user-id'];
        if (!userId) {
            return res.status(401).json({ error: '인증이 필요합니다.' });
        }

        const user = await models.User.findByPk(userId);
        if (!user || (user.role !== 'admin' && user.role !== 'super_admin')) {
            return res.status(403).json({ error: '관리자 권한이 필요합니다.' });
        }

        req.user = user;
        next();
    } catch (error) {
        console.error('권한 체크 실패:', error);
        res.status(500).json({ error: '권한 확인 중 오류가 발생했습니다.' });
    }
};
```

---

## 🎨 프론트엔드 변경사항

### 1. 새로운 페이지 구조

```
src/routes/profile/
├── admin/                    # 기존 admin (상품 관리자)
│   ├── AdminDashboard.js
│   ├── components/
│   └── ...
├── super-admin/              # 신규: 사이트 전체 관리자
│   ├── SuperAdminDashboard.js      # 메인 대시보드
│   ├── Products.js                  # 상품 관리 (어드민이 신청한 상품 승인)
│   ├── StudentVerifications.js      # 학생 인증 관리
│   ├── IPManagement.js              # IP 관리 (접속자 수, 통계, 데이터화)
│   ├── Security.js                  # 보안 관리 (봇 차단, 그래프화)
│   └── components/
│       ├── SuperAdminSidebar.js
│       ├── ProductApprovalList.js
│       ├── ProductApprovalDetail.js
│       ├── PendingVerificationList.js
│       ├── VerificationDetail.js
│       ├── IPAccessLogs.js
│       ├── IPStatistics.js
│       ├── SecurityEvents.js
│       ├── BotManagement.js
│       └── SecurityCharts.js
```

### 2. 라우팅 설정

**파일:** `Electronic-Commerce-Site/grab-market-web/src/App.js`

```javascript
import SuperAdminDashboard from "./routes/profile/super-admin/SuperAdminDashboard";
import SuperAdminProducts from "./routes/profile/super-admin/Products";
import SuperAdminStudentVerifications from "./routes/profile/super-admin/StudentVerifications";
import SuperAdminIPManagement from "./routes/profile/super-admin/IPManagement";
import SuperAdminSecurity from "./routes/profile/super-admin/Security";

// 라우트 추가
<ProtectedRoute 
  exact={true} 
  path="/profile/super-admin" 
  component={SuperAdminDashboard} 
  requireSuperAdmin={true} 
/>
<ProtectedRoute 
  exact={true} 
  path="/profile/super-admin/products" 
  component={SuperAdminProducts} 
  requireSuperAdmin={true} 
/>
<ProtectedRoute 
  exact={true} 
  path="/profile/super-admin/student-verifications" 
  component={SuperAdminStudentVerifications} 
  requireSuperAdmin={true} 
/>
<ProtectedRoute 
  exact={true} 
  path="/profile/super-admin/ip-management" 
  component={SuperAdminIPManagement} 
  requireSuperAdmin={true} 
/>
<ProtectedRoute 
  exact={true} 
  path="/profile/super-admin/security" 
  component={SuperAdminSecurity} 
  requireSuperAdmin={true} 
/>
```

### 3. 권한 체크 컴포넌트 수정

**파일:** `Electronic-Commerce-Site/grab-market-web/src/components/ProtectedRoute.js`

```javascript
// super_admin 권한 체크 로직 추가
const isSuperAdmin = user?.role === 'super_admin';
const isAdmin = user?.role === 'admin' || user?.role === 'super_admin';

if (requireSuperAdmin && !isSuperAdmin) {
  // 권한 없음 페이지로 리다이렉트
  return <Redirect to="/unauthorized" />;
}
```

### 4. 프로필 페이지 라우팅 수정

**파일:** `Electronic-Commerce-Site/grab-market-web/src/routes/profile/index.js`

```javascript
// role에 따라 다른 대시보드 표시
if (user.role === 'super_admin') {
  return <SuperAdminDashboard />;
} else if (user.role === 'admin') {
  return <AdminDashboard />;
}
// 일반 사용자 화면
```

### 5. API 클라이언트 추가

**파일:** `Electronic-Commerce-Site/grab-market-web/src/config/api.js`

```javascript
// 사이트 관리자 관련 API
superAdmin: {
  /**
   * 대기 중인 학생 인증 목록 조회
   */
  getPendingStudentVerifications: () => 
    apiClient.get('/api/users/student-verifications/pending'),

  /**
   * 학생 인증 승인
   */
  approveStudentVerification: (userId) => 
    apiClient.post(`/api/users/${userId}/student-verification/approve`),

  /**
   * 학생 인증 거부
   */
  rejectStudentVerification: (userId, reason) => 
    apiClient.post(`/api/users/${userId}/student-verification/reject`, { reason }),
},
```

---

## 📝 주요 기능 명세

### 1. 사이트 전체 관리자 대시보드

**기능:**
- 전체 사이트 관리 기능 통합
- 주요 통계 요약 (사용자, 주문, 상품, 보안 이벤트)
- 최근 활동 요약
- 빠른 액션 메뉴

**UI 구성:**
- 사이드바 네비게이션 (4개 주요 섹션)
- 메인 대시보드 (통계 카드, 최근 활동)
- 각 기능별 페이지

---

### 2. 상품 관리 페이지 (`/profile/super-admin/products`)

**목적:** 어드민(admin) 유저가 상품 등록을 신청하면 승인/거부하는 페이지

#### 기능
- **대기 중인 상품 신청 목록**
  - 상품명, 카테고리, 가격, 신청자(admin) 정보
  - 신청 일시, 상태 (대기 중/승인/거부)
  
- **상품 상세 정보 확인**
  - 상품 이미지, 설명, 태그 등 전체 정보 미리보기
  - 신청자(admin) 프로필 정보
  
- **승인/거부 처리**
  - 승인 버튼: 상품을 승인하고 공개
  - 거부 버튼: 거부 사유 입력 후 거부 처리
  - 일괄 승인/거부 기능 (선택된 항목)

- **필터링 및 검색**
  - 상태별 필터 (대기 중, 승인됨, 거부됨)
  - 카테고리별 필터
  - 신청자별 필터
  - 상품명 검색

- **통계**
  - 대기 중인 신청 수
  - 오늘 승인된 상품 수
  - 이번 주/월 승인 통계

#### 데이터베이스 변경사항

**Products 테이블에 필드 추가:**
```sql
ALTER TABLE products ADD COLUMN approval_status ENUM('pending', 'approved', 'rejected') DEFAULT 'pending';
ALTER TABLE products ADD COLUMN approval_requested_at DATETIME NULL COMMENT '승인 신청일시';
ALTER TABLE products ADD COLUMN approved_at DATETIME NULL COMMENT '승인일시';
ALTER TABLE products ADD COLUMN rejected_at DATETIME NULL COMMENT '거부일시';
ALTER TABLE products ADD COLUMN rejection_reason TEXT NULL COMMENT '거부 사유';
ALTER TABLE products ADD COLUMN approved_by INT NULL COMMENT '승인한 관리자 ID';
ALTER TABLE products ADD FOREIGN KEY (approved_by) REFERENCES users(id);
```

**기존 상품 처리:**
```sql
-- 기존 승인된 상품은 모두 approved로 설정
UPDATE products SET approval_status = 'approved' WHERE approval_status IS NULL;
```

#### API 엔드포인트

```javascript
// 대기 중인 상품 신청 목록 조회
GET /api/admin/products/pending
Query: ?page=1&limit=20&category=&status=pending&search=

// 상품 승인
POST /api/admin/products/:productId/approve
Body: { approved_by: userId }

// 상품 거부
POST /api/admin/products/:productId/reject
Body: { rejected_by: userId, reason: "거부 사유" }

// 상품 상세 정보 (승인 대기용)
GET /api/admin/products/:productId/pending-details
```

#### UI 구성
- **목록 화면**
  - 테이블: 상품명, 카테고리, 가격, 신청자, 신청일, 상태, 액션
  - 필터 바 (상태, 카테고리, 검색)
  - 페이지네이션
  
- **상세 화면 (모달 또는 별도 페이지)**
  - 상품 정보 미리보기 (이미지, 설명, 태그 등)
  - 신청자 정보
  - 승인/거부 버튼
  - 거부 사유 입력 필드 (거부 시)

---

### 3. 학생 인증 승인 페이지 (`/profile/super-admin/student-verifications`)

**목적:** 학생 인증 신청을 승인/거부하는 페이지

#### 기능
- **대기 중인 학생 인증 신청 목록**
  - 사용자명, 이메일, 신청 일시
  - 인증 문서 미리보기 링크
  
- **인증 문서 확인**
  - 업로드된 학생증/재학증명서 이미지 표시
  - 문서 다운로드 기능
  
- **승인/거부 처리**
  - 승인 버튼: 학생 인증 승인 (1년 유효기간 설정)
  - 거부 버튼: 거부 사유 입력 후 거부 처리
  - 일괄 승인/거부 기능

- **필터링 및 검색**
  - 상태별 필터 (대기 중, 승인됨, 거부됨)
  - 검색 (이름, 이메일)
  - 날짜 범위 필터

- **통계**
  - 대기 중인 신청 수
  - 오늘 승인된 인증 수
  - 승인율 통계

#### 데이터베이스 변경사항
```sql
-- Users 테이블에 이미 student_verification_document 필드가 있음
-- 추가 필드 (선택사항):
ALTER TABLE users ADD COLUMN verification_rejected_at DATETIME NULL;
ALTER TABLE users ADD COLUMN verification_rejection_reason TEXT NULL;
ALTER TABLE users ADD COLUMN verification_approved_by INT NULL;
ALTER TABLE users ADD FOREIGN KEY (verification_approved_by) REFERENCES users(id);
```

#### API 엔드포인트
```javascript
// 대기 중인 학생 인증 목록
GET /api/admin/student-verifications/pending

// 학생 인증 승인
POST /api/admin/users/:userId/student-verification/approve
Body: { approved_by: adminUserId }

// 학생 인증 거부
POST /api/admin/users/:userId/student-verification/reject
Body: { rejected_by: adminUserId, reason: "거부 사유" }

// 인증 문서 조회
GET /api/admin/users/:userId/verification-document
```

#### UI 구성
- **목록 화면**
  - 테이블: 이름, 이메일, 신청일, 문서, 상태, 액션
  - 필터 바 (상태, 검색)
  - 페이지네이션
  
- **상세 화면 (모달)**
  - 사용자 정보
  - 인증 문서 이미지 표시 (확대 가능)
  - 승인/거부 버튼
  - 거부 사유 입력 필드

---

### 4. IP 관리 페이지 (`/profile/super-admin/ip-management`)

**목적:** 접속자 IP 추적, 통계 관리, 데이터 분석

#### 기능
- **IP 접속 로그**
  - 접속 IP 주소, 접속 시간, 접속 페이지
  - 사용자 정보 (로그인한 경우)
  - 위치 정보 (국가, 도시 - IP Geolocation API 사용)
  - User-Agent 정보
  
- **접속 통계**
  - 일별/주별/월별 접속자 수
  - IP별 접속 횟수
  - 국가별 접속 분포
  - 시간대별 접속 분포 (시간대 그래프)
  - 페이지별 접속 통계
  
- **IP 관리**
  - 특정 IP 차단/해제
  - IP별 메모/태그 추가
  - IP 화이트리스트 관리
  
- **데이터 시각화**
  - 접속자 수 추이 그래프 (라인 차트)
  - 국가별 분포 파이 차트
  - 시간대별 히트맵
  - TOP 접속 IP 리스트

#### 데이터베이스 변경사항

**새 테이블 생성:**
```sql
CREATE TABLE ip_access_logs (
  id INT PRIMARY KEY AUTO_INCREMENT,
  ip_address VARCHAR(45) NOT NULL COMMENT 'IPv4 또는 IPv6',
  user_id INT NULL COMMENT '로그인한 사용자 ID (NULL 가능)',
  request_path VARCHAR(500) NOT NULL COMMENT '접속한 페이지 경로',
  request_method VARCHAR(10) NOT NULL COMMENT 'HTTP 메서드',
  user_agent TEXT NULL COMMENT 'User-Agent 헤더',
  country VARCHAR(100) NULL COMMENT '국가명',
  city VARCHAR(100) NULL COMMENT '도시명',
  is_blocked BOOLEAN DEFAULT FALSE COMMENT '차단 여부',
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  INDEX idx_ip (ip_address),
  INDEX idx_user (user_id),
  INDEX idx_created (created_at),
  INDEX idx_blocked (is_blocked),
  FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE SET NULL
);

CREATE TABLE ip_management (
  id INT PRIMARY KEY AUTO_INCREMENT,
  ip_address VARCHAR(45) NOT NULL UNIQUE,
  is_blocked BOOLEAN DEFAULT FALSE COMMENT '차단 여부',
  is_whitelisted BOOLEAN DEFAULT FALSE COMMENT '화이트리스트 여부',
  block_reason TEXT NULL COMMENT '차단 사유',
  memo TEXT NULL COMMENT '관리 메모',
  blocked_by INT NULL COMMENT '차단한 관리자 ID',
  blocked_at DATETIME NULL COMMENT '차단 일시',
  tags VARCHAR(255) NULL COMMENT '태그 (콤마 구분)',
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  INDEX idx_ip (ip_address),
  INDEX idx_blocked (is_blocked),
  INDEX idx_whitelist (is_whitelisted),
  FOREIGN KEY (blocked_by) REFERENCES users(id)
);
```

#### API 엔드포인트

```javascript
// 접속 로그 목록
GET /api/admin/ip-logs
Query: ?page=1&limit=50&ip=&country=&dateFrom=&dateTo=&blocked=

// 접속 통계
GET /api/admin/ip-logs/stats
Query: ?period=daily|weekly|monthly&dateFrom=&dateTo=

// 국가별 통계
GET /api/admin/ip-logs/stats/countries
Query: ?dateFrom=&dateTo=

// 시간대별 통계
GET /api/admin/ip-logs/stats/hourly
Query: ?dateFrom=&dateTo=

// IP 관리 (차단/해제)
POST /api/admin/ip-management/block
Body: { ip_address, reason, memo }

POST /api/admin/ip-management/unblock
Body: { ip_address }

POST /api/admin/ip-management/whitelist
Body: { ip_address, memo }

// IP 관리 목록
GET /api/admin/ip-management
Query: ?page=1&limit=50&blocked=&whitelisted=&search=
```

#### UI 구성
- **접속 로그 탭**
  - 테이블: IP, 사용자, 위치, 페이지, 시간, 상태
  - 필터 (IP, 국가, 날짜 범위, 차단 여부)
  - 페이지네이션
  
- **통계 탭**
  - 접속자 수 추이 그래프 (날짜별)
  - 국가별 분포 차트
  - 시간대별 히트맵
  - TOP IP 리스트
  
- **IP 관리 탭**
  - IP 목록 (차단/화이트리스트 상태 표시)
  - IP 검색 및 필터
  - 차단/해제 버튼
  - 메모 추가 기능

---

### 5. 보안 페이지 (`/profile/super-admin/security`)

**목적:** 봇 차단, 보안 이벤트 관리, 그래프화 및 데이터 분석

#### 기능
- **봇 탐지 및 차단**
  - 자동 봇 탐지 (User-Agent 분석, 행동 패턴 분석)
  - 의심스러운 IP 자동 차단
  - 수동 봇 차단/해제
  
- **보안 이벤트 로그**
  - 로그인 실패 기록
  - 비정상적인 접근 시도
  - API 남용 시도
  - 스크래핑 시도 감지
  
- **차단 IP 관리**
  - 차단된 IP 목록
  - 차단 사유별 분류
  - 차단 해제 기능
  - 자동/수동 차단 구분
  
- **보안 통계 및 그래프**
  - 일별 보안 이벤트 수 (라인 차트)
  - 이벤트 유형별 분포 (파이 차트)
  - 차단 IP 수 추이
  - 봇 탐지율 통계
  - 시간대별 공격 패턴 (히트맵)
  
- **보안 설정**
  - 자동 봇 차단 활성화/비활성화
  - 차단 임계값 설정
  - 알림 설정

#### 데이터베이스 변경사항

**새 테이블 생성:**
```sql
CREATE TABLE security_events (
  id INT PRIMARY KEY AUTO_INCREMENT,
  event_type ENUM('login_failed', 'bot_detected', 'api_abuse', 'scraping', 'suspicious_activity', 'ip_blocked') NOT NULL,
  ip_address VARCHAR(45) NOT NULL,
  user_id INT NULL COMMENT '관련 사용자 ID',
  request_path VARCHAR(500) NULL,
  user_agent TEXT NULL,
  details JSON NULL COMMENT '추가 세부 정보',
  severity ENUM('low', 'medium', 'high', 'critical') DEFAULT 'medium',
  is_blocked BOOLEAN DEFAULT FALSE,
  blocked_at DATETIME NULL,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  INDEX idx_type (event_type),
  INDEX idx_ip (ip_address),
  INDEX idx_created (created_at),
  INDEX idx_severity (severity),
  FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE SET NULL
);

CREATE TABLE bot_detections (
  id INT PRIMARY KEY AUTO_INCREMENT,
  ip_address VARCHAR(45) NOT NULL,
  user_agent TEXT NULL,
  detection_reason TEXT NOT NULL COMMENT '탐지 사유',
  confidence_score DECIMAL(5,2) DEFAULT 0.00 COMMENT '탐지 신뢰도 (0-100)',
  is_blocked BOOLEAN DEFAULT FALSE,
  blocked_at DATETIME NULL,
  blocked_by INT NULL COMMENT '차단한 관리자 ID (자동 차단 시 NULL)',
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  INDEX idx_ip (ip_address),
  INDEX idx_blocked (is_blocked),
  INDEX idx_created (created_at),
  FOREIGN KEY (blocked_by) REFERENCES users(id)
);
```

#### API 엔드포인트

```javascript
// 보안 이벤트 목록
GET /api/admin/security/events
Query: ?page=1&limit=50&type=&severity=&dateFrom=&dateTo=&ip=

// 보안 이벤트 통계
GET /api/admin/security/events/stats
Query: ?period=daily|weekly|monthly&dateFrom=&dateTo=

// 이벤트 유형별 통계
GET /api/admin/security/events/stats/by-type
Query: ?dateFrom=&dateTo=

// 봇 탐지 목록
GET /api/admin/security/bots
Query: ?page=1&limit=50&blocked=&dateFrom=&dateTo=

// 봇 차단/해제
POST /api/admin/security/bots/:id/block
Body: { reason }

POST /api/admin/security/bots/:id/unblock

// IP 수동 차단
POST /api/admin/security/block-ip
Body: { ip_address, reason, duration_hours }

// IP 차단 해제
POST /api/admin/security/unblock-ip
Body: { ip_address }

// 보안 설정 조회/수정
GET /api/admin/security/settings
PUT /api/admin/security/settings
Body: {
  auto_block_enabled: true,
  bot_detection_threshold: 70,
  max_login_attempts: 5,
  block_duration_hours: 24
}
```

#### UI 구성
- **보안 이벤트 탭**
  - 이벤트 목록 테이블 (유형, IP, 심각도, 시간, 상세)
  - 필터 (유형, 심각도, 날짜, IP)
  - 이벤트 상세 모달
  
- **봇 관리 탭**
  - 봇 탐지 목록 (IP, User-Agent, 탐지 사유, 신뢰도, 상태)
  - 차단/해제 버튼
  - 봇 패턴 분석 정보
  
- **차단 IP 관리 탭**
  - 차단된 IP 목록
  - 차단 사유, 차단 시간, 차단 유형 (자동/수동)
  - 해제 버튼
  
- **통계 및 그래프 탭**
  - 보안 이벤트 추이 그래프
  - 이벤트 유형별 분포 차트
  - 차단 IP 수 추이
  - 시간대별 공격 패턴 히트맵
  
- **설정 탭**
  - 자동 봇 차단 토글
  - 차단 임계값 설정
  - 알림 설정

### 3. 일반 관리자 관리 페이지

**기능:**
- admin 권한 사용자 목록
- admin 계정 생성/수정/삭제
- 권한 변경 (admin ↔ user)

**UI 구성:**
- 관리자 목록 테이블
- 추가/수정/삭제 버튼
- 권한 변경 드롭다운

### 4. 사이트 설정 페이지

**기능:**
- 사이트 기본 정보 수정
- 이메일 설정
- 시스템 설정 (할인율, 수수료 등)

---

## 🗂️ 구현 순서

### Phase 1: 데이터베이스 및 백엔드 기본 설정
1. ✅ User 모델 role ENUM 수정
2. ✅ 데이터베이스 마이그레이션 스크립트 작성
3. ✅ 권한 체크 미들웨어 구현
4. ✅ 학생 인증 관리 API 구현

### Phase 2: 프론트엔드 기본 구조
1. ✅ SuperAdminDashboard 컴포넌트 생성
2. ✅ SuperAdminSidebar 컴포넌트 생성
3. ✅ 라우팅 설정
4. ✅ ProtectedRoute 권한 체크 로직 추가

### Phase 3: 학생 인증 관리 기능
1. ✅ StudentVerifications 페이지 생성
2. ✅ PendingVerificationList 컴포넌트 생성
3. ✅ VerificationDetail 컴포넌트 생성 (문서 미리보기)
4. ✅ 승인/거부 기능 구현

### Phase 4: 상품 관리 기능
1. ✅ Products 테이블에 approval 관련 필드 추가
2. ✅ 상품 승인 API 구현
3. ✅ SuperAdminProducts 페이지 생성
4. ✅ 상품 승인/거부 UI 구현

### Phase 5: IP 관리 기능
1. ✅ ip_access_logs 테이블 생성
2. ✅ ip_management 테이블 생성
3. ✅ IP 접속 로그 기록 미들웨어 구현
4. ✅ IP 관리 API 구현
5. ✅ SuperAdminIPManagement 페이지 생성
6. ✅ 통계 및 그래프 구현

### Phase 6: 보안 관리 기능
1. ✅ security_events 테이블 생성
2. ✅ bot_detections 테이블 생성
3. ✅ 봇 탐지 로직 구현
4. ✅ 보안 이벤트 로깅 미들웨어 구현
5. ✅ 보안 API 구현
6. ✅ SuperAdminSecurity 페이지 생성
7. ✅ 보안 통계 및 그래프 구현

---

## 📌 주의사항

1. **기존 데이터 호환성**
   - 기존 `admin` 권한 사용자는 그대로 유지
   - 필요시 수동으로 `super_admin`으로 변경

2. **보안**
   - 모든 관리자 API에 권한 체크 미들웨어 적용
   - JWT 토큰 기반 인증 구현 권장
   - 민감한 작업 로그 기록

3. **사용자 경험**
   - 권한별로 접근 가능한 메뉴만 표시
   - 권한 없는 페이지 접근 시 적절한 안내

4. **확장성**
   - 향후 추가 권한 레벨 확장 가능하도록 구조 설계
   - 권한 관리 시스템 고려 (RBAC)

---

## 🔄 마이그레이션 가이드

### 1. 데이터베이스 마이그레이션

```sql
-- Users 테이블의 role 컬럼 수정
ALTER TABLE users 
MODIFY COLUMN role ENUM('super_admin', 'admin', 'user') DEFAULT 'user';

-- 기존 admin 중 하나를 super_admin으로 변경 (예시)
UPDATE users SET role = 'super_admin' WHERE id = 1;
```

### 2. 백엔드 배포

1. User 모델 수정
2. 새로운 API 엔드포인트 추가
3. 권한 체크 미들웨어 적용
4. 서버 재시작

### 3. 프론트엔드 배포

1. 새로운 컴포넌트 및 페이지 추가
2. 라우팅 설정
3. API 클라이언트 추가
4. 권한 체크 로직 추가
5. 빌드 및 배포

---

## ✅ 체크리스트

### 데이터베이스
- [ ] Users 테이블 role ENUM 수정
- [ ] Products 테이블 approval 관련 필드 추가
- [ ] ip_access_logs 테이블 생성
- [ ] ip_management 테이블 생성
- [ ] security_events 테이블 생성
- [ ] bot_detections 테이블 생성
- [ ] 마이그레이션 스크립트 작성
- [ ] 테스트 데이터 준비

### 백엔드
- [ ] User 모델 수정
- [ ] Product 모델 approval 필드 추가
- [ ] 학생 인증 관리 API 구현
- [ ] 상품 승인 관리 API 구현
- [ ] IP 로깅 미들웨어 구현
- [ ] IP 관리 API 구현
- [ ] 봇 탐지 로직 구현
- [ ] 보안 이벤트 로깅 미들웨어 구현
- [ ] 보안 관리 API 구현
- [ ] 권한 체크 미들웨어 구현
- [ ] 라우트 설정
- [ ] API 문서 업데이트

### 프론트엔드
- [ ] SuperAdminDashboard 생성
- [ ] StudentVerifications 페이지 생성
- [ ] AdminManagement 페이지 생성
- [ ] SiteSettings 페이지 생성
- [ ] 라우팅 설정
- [ ] ProtectedRoute 권한 체크 추가
- [ ] API 클라이언트 추가

### 테스트
- [ ] 권한별 접근 제어 테스트
- [ ] 학생 인증 승인/거부 테스트
- [ ] 상품 승인/거부 테스트
- [ ] IP 로깅 및 관리 테스트
- [ ] 봇 탐지 및 차단 테스트
- [ ] 보안 이벤트 로깅 테스트
- [ ] 통계 및 그래프 표시 테스트
- [ ] UI/UX 테스트

---

이 문서를 기반으로 단계별로 구현을 진행하겠습니다.

