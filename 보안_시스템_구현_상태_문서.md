# 보안 시스템 구현 상태 문서

## 📋 개요

본 문서는 AIDE Market 사이트의 보안 시스템의 구현 상태를 상세히 설명합니다. 구현된 기능과 향후 구현이 필요한 기능을 구분하여 정리했습니다.

---

## ✅ 구현된 기능

### 1. 데이터베이스 모델

#### 1.1 IpAccessLog 모델
**위치:** `features/security/models/IpAccessLog.js`

**목적:** 모든 접속 요청의 IP 주소를 로깅

**저장 정보:**
- `ip_address`: 접속 IP 주소 (IPv4/IPv6)
- `user_id`: 로그인한 사용자 ID (선택적)
- `request_path`: 접속한 페이지 경로
- `request_method`: HTTP 메서드 (GET, POST 등)
- `user_agent`: User-Agent 헤더
- `country`: 국가명 (추후 Geolocation API로 채움)
- `city`: 도시명 (추후 Geolocation API로 채움)
- `is_blocked`: 차단 여부
- `created_at`: 접속 시간

**현재 상태:** ✅ 완전 구현

#### 1.2 IpManagement 모델
**위치:** `features/security/models/IpManagement.js`

**목적:** IP 주소별 차단/화이트리스트 관리

**저장 정보:**
- `ip_address`: IP 주소 (UNIQUE)
- `is_blocked`: 차단 여부
- `is_whitelisted`: 화이트리스트 여부
- `block_reason`: 차단 사유
- `memo`: 관리 메모
- `blocked_by`: 차단한 관리자 ID
- `blocked_at`: 차단 일시
- `tags`: 태그 (콤마 구분)

**현재 상태:** ✅ 완전 구현

#### 1.3 SecurityEvent 모델
**위치:** `features/security/models/SecurityEvent.js`

**목적:** 보안 관련 이벤트 로깅

**저장 정보:**
- `event_type`: 이벤트 유형
  - `login_failed`: 로그인 실패
  - `bot_detected`: 봇 탐지
  - `api_abuse`: API 남용
  - `scraping`: 스크래핑 시도
  - `suspicious_activity`: 의심스러운 활동
  - `ip_blocked`: IP 차단
- `ip_address`: 관련 IP 주소
- `user_id`: 관련 사용자 ID (선택적)
- `request_path`: 요청 경로
- `user_agent`: User-Agent 헤더
- `details`: 추가 세부 정보 (JSON)
- `severity`: 심각도 (`low`, `medium`, `high`, `critical`)
- `is_blocked`: 차단 여부
- `blocked_at`: 차단 일시
- `created_at`: 이벤트 발생 시간

**현재 상태:** ✅ 모델 완전 구현, ⚠️ 자동 로깅 미구현

#### 1.4 BotDetection 모델
**위치:** `features/security/models/BotDetection.js`

**목적:** 봇 탐지 기록

**저장 정보:**
- `ip_address`: 봇 IP 주소
- `user_agent`: User-Agent
- `detection_reason`: 탐지 사유
- `confidence_score`: 탐지 신뢰도 (0-100)
- `is_blocked`: 차단 여부
- `blocked_at`: 차단 일시
- `blocked_by`: 차단한 관리자 ID
- `created_at`: 탐지 시간

**현재 상태:** ✅ 모델 완전 구현, ⚠️ 실제 봇 탐지 로직 미구현

---

### 2. IP 로깅 미들웨어

#### 2.1 ipLogger.js
**위치:** `middleware/ipLogger.js`

**기능:**
- 모든 HTTP 요청의 IP 주소를 자동으로 `IpAccessLog` 테이블에 저장
- 프록시 환경 고려 (X-Forwarded-For, CF-Connecting-IP 헤더 처리)
- 비동기 처리로 요청 흐름에 영향 없음
- 로깅 실패 시에도 요청은 정상 처리

**적용 위치:**
- `app/server.js`에 전역 미들웨어로 적용

**현재 상태:** ✅ 완전 구현 및 적용

**코드 예시:**
```javascript
// 모든 요청에 자동으로 IP 로그 저장
app.use(ipLogger.logIpAccess);
```

---

### 3. 백엔드 API

#### 3.1 IP 관리 API
**위치:** `features/security/ipManagementService.js`, `ipManagementController.js`, `ipManagementRoutes.js`

**구현된 엔드포인트:**

| 메서드 | 경로 | 기능 | 상태 |
|--------|------|------|------|
| GET | `/api/admin/ip/logs` | IP 접속 로그 목록 조회 | ✅ |
| GET | `/api/admin/ip/logs/stats` | 접속 통계 (일별/주별/월별) | ✅ |
| GET | `/api/admin/ip/logs/stats/countries` | 국가별 통계 | ✅ |
| GET | `/api/admin/ip/logs/stats/hourly` | 시간대별 통계 | ✅ |
| POST | `/api/admin/ip/management/block` | IP 차단 | ✅ |
| POST | `/api/admin/ip/management/unblock` | IP 차단 해제 | ✅ |
| POST | `/api/admin/ip/management/whitelist` | IP 화이트리스트 추가 | ✅ |
| GET | `/api/admin/ip/management` | IP 관리 목록 조회 | ✅ |

**권한:** `super_admin`만 접근 가능

**현재 상태:** ✅ 완전 구현

#### 3.2 보안 이벤트 관리 API
**위치:** `features/security/securityService.js`, `securityController.js`, `securityRoutes.js`

**구현된 엔드포인트:**

| 메서드 | 경로 | 기능 | 상태 |
|--------|------|------|------|
| GET | `/api/admin/security/events` | 보안 이벤트 목록 조회 | ✅ |
| GET | `/api/admin/security/events/stats` | 보안 이벤트 통계 | ✅ |
| GET | `/api/admin/security/events/stats/by-type` | 이벤트 유형별 통계 | ✅ |
| GET | `/api/admin/security/bots` | 봇 탐지 목록 조회 | ✅ |
| POST | `/api/admin/security/bots/:id/block` | 봇 차단 | ✅ |
| POST | `/api/admin/security/bots/:id/unblock` | 봇 차단 해제 | ✅ |
| POST | `/api/admin/security/block-ip` | IP 수동 차단 | ✅ |
| POST | `/api/admin/security/unblock-ip` | IP 수동 차단 해제 | ✅ |
| GET | `/api/admin/security/settings` | 보안 설정 조회 | ✅ |
| PUT | `/api/admin/security/settings` | 보안 설정 업데이트 | ✅ |

**권한:** `super_admin`만 접근 가능

**현재 상태:** ✅ API 완전 구현, ⚠️ 실제 이벤트 자동 로깅 미구현

---

### 4. 프론트엔드

#### 4.1 보안 관리 페이지
**위치:** `src/routes/profile/super-admin/Security.js`

**구현된 기능:**
- ✅ 보안 이벤트 목록 조회 및 표시
- ✅ 이벤트 필터링 (유형, 심각도, IP)
- ✅ 봇 탐지 목록 조회 및 표시
- ✅ 봇 수동 차단/해제
- ✅ 보안 설정 조회 및 수정
  - 자동 봇 차단 활성화/비활성화
  - 봇 탐지 임계값 설정
  - 최대 로그인 시도 횟수 설정
  - 차단 지속 시간 설정

**현재 상태:** ✅ 완전 구현 (백엔드 API와 연결)

#### 4.2 IP 관리 페이지
**위치:** `src/routes/profile/super-admin/IPManagement.js`

**구현된 기능:**
- ✅ IP 접속 로그 목록 조회 및 표시
- ✅ IP 필터링 (IP 주소, 국가, 날짜)
- ✅ IP 차단/해제 기능
- ✅ IP 화이트리스트 관리
- ✅ 통계 탭 UI (데이터 시각화 미구현)

**현재 상태:** ✅ 완전 구현 (백엔드 API와 연결)

---

## ⚠️ 미구현 기능 (향후 구현 필요)

### 1. 자동 보안 이벤트 로깅

#### 1.1 로그인 실패 추적
**필요한 기능:**
- 로그인 API에서 실패 시 `SecurityEvent` 자동 로깅
- 같은 IP에서 연속 로그인 실패 횟수 추적
- 설정된 임계값 초과 시 자동 IP 차단

**구현 위치:**
- `features/auth/authController.js` (로그인 컨트롤러)
- `middleware/securityMiddleware.js` (신규 생성)

**구현 방법:**
```javascript
// 로그인 실패 시
await SecurityEvent.create({
  event_type: 'login_failed',
  ip_address: req.ip,
  user_id: null,
  request_path: '/api/auth/login',
  user_agent: req.headers['user-agent'],
  severity: 'medium',
  details: { username: req.body.username }
});

// 연속 실패 횟수 확인
const recentFailures = await SecurityEvent.count({
  where: {
    event_type: 'login_failed',
    ip_address: req.ip,
    created_at: { [Op.gte]: new Date(Date.now() - 3600000) } // 최근 1시간
  }
});

if (recentFailures >= settings.max_login_attempts) {
  // 자동 IP 차단
  await ipManagementService.blockIP(req.ip, null, '로그인 실패 횟수 초과');
  await SecurityEvent.create({
    event_type: 'ip_blocked',
    ip_address: req.ip,
    severity: 'high',
    details: { reason: '로그인 실패 횟수 초과', failures: recentFailures }
  });
}
```

**우선순위:** 🔴 높음

---

#### 1.2 봇 자동 탐지
**필요한 기능:**
- User-Agent 분석을 통한 봇 탐지
- 비정상적인 요청 패턴 감지
- 신뢰도 점수 계산
- 설정된 임계값 이상 시 자동 차단

**구현 위치:**
- `middleware/botDetection.js` (신규 생성)

**탐지 기준:**
1. **User-Agent 패턴:**
   - 일반적인 봇 User-Agent (curl, wget, python-requests, etc.)
   - User-Agent 누락
   - 비정상적인 User-Agent 형식

2. **요청 패턴:**
   - 초당 요청 수가 비정상적으로 많음
   - 특정 엔드포인트에 집중된 요청
   - 일반 사용자가 접근하지 않는 경로 반복 접근

3. **행동 패턴:**
   - 쿠키/세션 미사용
   - JavaScript 실행 불가 (Headless 브라우저 제외)

**구현 방법:**
```javascript
// botDetection.js
const botPatterns = [
  /curl/i,
  /wget/i,
  /python-requests/i,
  /bot/i,
  /crawler/i,
  /spider/i
];

const detectBot = (req) => {
  const userAgent = req.headers['user-agent'] || '';
  let confidence = 0;
  let reason = '';

  // User-Agent 패턴 매칭
  for (const pattern of botPatterns) {
    if (pattern.test(userAgent)) {
      confidence += 40;
      reason += `User-Agent 패턴 매칭: ${pattern}; `;
    }
  }

  // User-Agent 누락
  if (!userAgent) {
    confidence += 30;
    reason += 'User-Agent 누락; ';
  }

  // 요청 빈도 체크 (추가 구현 필요)
  // IP별 요청 수를 카운트하여 비정상적으로 높으면 confidence 증가

  return { confidence, reason: reason.trim() };
};
```

**우선순위:** 🔴 높음

---

#### 1.3 API 남용 탐지
**필요한 기능:**
- 동일 IP에서 짧은 시간 내 과도한 API 요청 감지
- Rate limiting 위반 감지
- 비정상적인 API 호출 패턴 감지

**구현 위치:**
- `middleware/rateLimiter.js` (신규 생성)
- `middleware/securityMiddleware.js`

**구현 방법:**
```javascript
// rateLimiter.js
const rateLimitStore = new Map(); // Redis 사용 권장

const checkRateLimit = async (ip, endpoint, limit = 100, window = 60000) => {
  const key = `${ip}:${endpoint}`;
  const now = Date.now();
  
  if (!rateLimitStore.has(key)) {
    rateLimitStore.set(key, { count: 1, resetTime: now + window });
    return true;
  }

  const record = rateLimitStore.get(key);
  
  if (now > record.resetTime) {
    record.count = 1;
    record.resetTime = now + window;
    return true;
  }

  if (record.count >= limit) {
    // API 남용 이벤트 로깅
    await SecurityEvent.create({
      event_type: 'api_abuse',
      ip_address: ip,
      request_path: endpoint,
      severity: 'high',
      details: { requestCount: record.count, limit }
    });
    return false;
  }

  record.count++;
  return true;
};
```

**우선순위:** 🟡 중간

---

#### 1.4 스크래핑 탐지
**필요한 기능:**
- 특정 페이지에 대한 집중적인 접근 감지
- 로봇 배제 표준(robots.txt) 위반 감지
- 일반 사용자와 다른 접근 패턴 감지

**구현 위치:**
- `middleware/scrapingDetection.js` (신규 생성)

**탐지 기준:**
- 동일 IP에서 짧은 시간 내 다수의 상품 페이지 접근
- 연속적인 페이지 번호 접근 패턴
- API 엔드포인트에 대한 순차적 접근

**우선순위:** 🟡 중간

---

### 2. 자동 IP 차단 시스템

#### 2.1 자동 차단 로직
**필요한 기능:**
- 보안 설정에 따른 자동 차단
- 봇 탐지 신뢰도가 임계값 이상일 때 자동 차단
- 로그인 실패 횟수 초과 시 자동 차단
- API 남용 시 자동 차단

**구현 위치:**
- `services/autoBlockService.js` (신규 생성)

**구현 방법:**
```javascript
// autoBlockService.js
exports.autoBlockIP = async (ipAddress, reason, severity = 'medium') => {
  const settings = await getSecuritySettings();
  
  if (!settings.auto_block_enabled) {
    return false;
  }

  // 이미 차단되어 있는지 확인
  const existing = await IpManagement.findOne({ where: { ip_address: ipAddress } });
  if (existing && existing.is_blocked) {
    return true;
  }

  // 차단 처리
  await ipManagementService.blockIP(
    ipAddress,
    null, // 자동 차단이므로 blocked_by는 null
    reason,
    `자동 차단: ${reason}`
  );

  // 보안 이벤트 로깅
  await SecurityEvent.create({
    event_type: 'ip_blocked',
    ip_address: ipAddress,
    severity,
    is_blocked: true,
    blocked_at: new Date(),
    details: { reason, auto: true }
  });

  return true;
};
```

**우선순위:** 🔴 높음

---

#### 2.2 자동 차단 해제
**필요한 기능:**
- 설정된 시간 후 자동 차단 해제
- 스케줄러를 통한 주기적 확인

**구현 위치:**
- `jobs/autoUnblockScheduler.js` (신규 생성)

**구현 방법:**
```javascript
// autoUnblockScheduler.js
const cron = require('node-cron');

// 매 시간마다 실행
cron.schedule('0 * * * *', async () => {
  const settings = await getSecuritySettings();
  const blockDurationMs = settings.block_duration_hours * 3600000;

  // 차단된 IP 중 차단 시간이 지난 것들 찾기
  const expiredBlocks = await IpManagement.findAll({
    where: {
      is_blocked: true,
      blocked_at: {
        [Op.lte]: new Date(Date.now() - blockDurationMs)
      }
    }
  });

  for (const ip of expiredBlocks) {
    await ipManagementService.unblockIP(ip.ip_address);
    
    // 이벤트 로깅
    await SecurityEvent.create({
      event_type: 'ip_blocked',
      ip_address: ip.ip_address,
      details: { action: 'auto_unblock', reason: '차단 시간 만료' }
    });
  }
});
```

**우선순위:** 🟡 중간

---

### 3. IP Geolocation

#### 3.1 위치 정보 자동 추가
**필요한 기능:**
- IP 주소를 국가/도시 정보로 변환
- `IpAccessLog`에 자동으로 국가/도시 정보 저장

**구현 방법:**
- 무료 API: ip-api.com, ip-api.io
- 유료 API: MaxMind GeoIP2, ipinfo.io

**구현 위치:**
- `services/geolocationService.js` (신규 생성)
- `middleware/ipLogger.js` 수정

**구현 방법:**
```javascript
// geolocationService.js
const axios = require('axios');

exports.getLocation = async (ipAddress) => {
  try {
    // 무료 API 사용 (예: ip-api.com)
    const response = await axios.get(`http://ip-api.com/json/${ipAddress}?fields=country,city`);
    
    if (response.data.status === 'success') {
      return {
        country: response.data.country,
        city: response.data.city
      };
    }
  } catch (error) {
    console.error('Geolocation API 실패:', error);
  }
  
  return { country: null, city: null };
};
```

**우선순위:** 🟢 낮음

---

### 4. 데이터 시각화

#### 4.1 보안 이벤트 통계 그래프
**필요한 기능:**
- 일별/주별/월별 보안 이벤트 수 추이 (라인 차트)
- 이벤트 유형별 분포 (파이 차트)
- 시간대별 공격 패턴 (히트맵)
- 차단 IP 수 추이

**구현 위치:**
- `src/routes/profile/super-admin/Security.js`

**라이브러리:**
- Recharts (이미 설치됨)
- Chart.js (이미 설치됨)

**우선순위:** 🟡 중간

---

#### 4.2 IP 접속 통계 그래프
**필요한 기능:**
- 일별/주별/월별 접속자 수 추이
- 국가별 분포 파이 차트
- 시간대별 히트맵
- TOP 접속 IP 리스트

**구현 위치:**
- `src/routes/profile/super-admin/IPManagement.js`

**우선순위:** 🟡 중간

---

### 5. 실시간 알림 시스템

#### 5.1 보안 이벤트 알림
**필요한 기능:**
- 심각한 보안 이벤트 발생 시 관리자에게 알림
- WebSocket 또는 Server-Sent Events 사용
- 이메일 알림 (선택적)

**구현 방법:**
- WebSocket으로 실시간 업데이트
- 설정된 심각도 이상의 이벤트 발생 시 알림

**우선순위:** 🟢 낮음

---

### 6. 차단 IP 미들웨어

#### 6.1 요청 차단 미들웨어
**필요한 기능:**
- 차단된 IP에서 오는 요청 자동 거부
- 화이트리스트 IP는 항상 허용
- 차단된 요청에 대한 응답 (403 Forbidden)

**구현 위치:**
- `middleware/ipBlockMiddleware.js` (신규 생성)

**구현 방법:**
```javascript
// ipBlockMiddleware.js
exports.checkIPBlock = async (req, res, next) => {
  const ipAddress = req.headers['cf-connecting-ip'] 
                 || req.headers['x-forwarded-for']?.split(',')[0]?.trim()
                 || req.ip;

  // 화이트리스트 확인
  const whitelisted = await IpManagement.findOne({
    where: {
      ip_address: ipAddress,
      is_whitelisted: true
    }
  });

  if (whitelisted) {
    return next();
  }

  // 차단 확인
  const blocked = await IpManagement.findOne({
    where: {
      ip_address: ipAddress,
      is_blocked: true
    }
  });

  if (blocked) {
    // 접근 거부 이벤트 로깅
    await SecurityEvent.create({
      event_type: 'ip_blocked',
      ip_address: ipAddress,
      request_path: req.path,
      severity: 'low',
      details: { action: 'access_denied' }
    });

    return res.status(403).json({
      success: false,
      error: 'アクセスが拒否されました。このIPアドレスはブロックされています。'
    });
  }

  next();
};
```

**적용 위치:**
- `app/server.js`에서 IP 로깅 이후, 라우트 처리 전에 적용

**우선순위:** 🔴 높음

---

## 📊 구현 상태 요약

| 기능 | 백엔드 | 프론트엔드 | 상태 |
|------|--------|-----------|------|
| IP 로깅 | ✅ | ✅ | 완전 구현 |
| IP 관리 (수동) | ✅ | ✅ | 완전 구현 |
| 보안 이벤트 모델 | ✅ | ✅ | 완전 구현 |
| 보안 이벤트 조회 | ✅ | ✅ | 완전 구현 |
| 봇 탐지 모델 | ✅ | ✅ | 완전 구현 |
| 봇 관리 (수동) | ✅ | ✅ | 완전 구현 |
| 보안 설정 | ✅ | ✅ | 완전 구현 |
| 로그인 실패 추적 | ❌ | - | 미구현 |
| 자동 봇 탐지 | ❌ | - | 미구현 |
| 자동 IP 차단 | ❌ | - | 미구현 |
| 자동 차단 해제 | ❌ | - | 미구현 |
| API Rate Limiting | ❌ | - | 미구현 |
| 스크래핑 탐지 | ❌ | - | 미구현 |
| IP Geolocation | ❌ | - | 미구현 |
| 차단 IP 미들웨어 | ❌ | - | 미구현 |
| 데이터 시각화 | ⚠️ | ⚠️ | 부분 구현 |
| 실시간 알림 | ❌ | ❌ | 미구현 |

---

## 🎯 구현 우선순위

### Phase 1: 핵심 보안 기능 (우선순위 높음)
1. **차단 IP 미들웨어** - 차단된 IP의 요청을 실제로 차단
2. **로그인 실패 추적** - 로그인 보안 강화
3. **자동 봇 탐지** - 기본적인 봇 탐지 로직
4. **자동 IP 차단** - 설정에 따른 자동 차단

### Phase 2: 고급 보안 기능 (우선순위 중간)
5. **API Rate Limiting** - API 남용 방지
6. **자동 차단 해제** - 시간 경과 후 자동 해제
7. **스크래핑 탐지** - 데이터 수집 방지
8. **데이터 시각화** - 통계 그래프 및 차트

### Phase 3: 부가 기능 (우선순위 낮음)
9. **IP Geolocation** - 위치 정보 자동 추가
10. **실시간 알림** - WebSocket을 통한 실시간 업데이트

---

## 🔒 보안 설정

현재 구현된 보안 설정은 메모리에 저장되며, 실제로는 데이터베이스나 설정 파일에 저장해야 합니다.

**개선 필요사항:**
- 보안 설정을 데이터베이스에 저장
- 설정 변경 이력 추적
- 설정 백업/복원 기능

**권장 테이블 구조:**
```sql
CREATE TABLE security_settings (
  id INT PRIMARY KEY AUTO_INCREMENT,
  setting_key VARCHAR(100) UNIQUE NOT NULL,
  setting_value TEXT NOT NULL,
  updated_by INT NULL,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  FOREIGN KEY (updated_by) REFERENCES users(id)
);
```

---

## 📝 참고사항

1. **성능 고려사항:**
   - IP 로깅은 비동기로 처리하여 요청 속도에 영향 없음
   - 차단 IP 확인은 캐싱 필요 (Redis 권장)
   - 대량 로그는 주기적으로 아카이빙 필요

2. **확장성:**
   - 현재는 메모리 기반 Rate Limiting 사용
   - 프로덕션 환경에서는 Redis 사용 권장
   - 분산 환경에서는 공유 스토리지 필요

3. **보안:**
   - 민감한 정보 (IP 주소, 사용자 정보)는 암호화 저장 고려
   - 로그 접근 권한 제한
   - 정기적인 보안 감사 필요

---

이 문서는 구현이 진행됨에 따라 지속적으로 업데이트되어야 합니다.

